<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/CSS/style.css" />
    <title>Weather Forecast</title>
  </head>
  <body>
    <div class="before">
      <div class="head">
        <h1>LIVE WEATHER</h1>
        <p>Stepping out?? Oh wait, It's time to do a weather check</p>
      </div>

      <form action="/" method="post">
        <div id="form">
          <div class="warning">
            <div>
              <input
                type="text"
                id="searchbox"
                name="cityName"
                placeholder="Enter city name"
              />
            </div>
            <div class="error">
              <% if(locals.note){ %>
              <div class="error"><%= note%></div>
              <% } %>
            </div>
          </div>
          <div><button id="searchbtn" type="submit">Search</button></div>
        </div>
      </form>
    </div>
  </body>
</html>
